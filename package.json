{
  "name": "cloud-memory",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud Memory - MERN image memory app",
  "scripts": {
    "build": "npm ci --prefix frontend && npm run build --prefix frontend && npm ci --prefix backend --omit=dev",
    "start": "node backend/src/server.js"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
