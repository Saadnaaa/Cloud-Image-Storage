{
  "name": "cloud-memory",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend && npm start --prefix backend"
  }
}
